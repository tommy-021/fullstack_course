
import { DateRecord} from './types.ts';

export type EventProps = { location?: string, id: string, title: string, dates: DateRecord[];

export const Event: React.FC<EventProps> = (props) => {
    return
}
